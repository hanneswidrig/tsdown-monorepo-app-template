{
  "name": "tsdown-monorepo-app-template",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "deps": "pnpm up -r --latest --interactive",
    "build": "tsdown",
    "dev": "tsdown --watch",
    "typecheck": "tsgo",
    "prepublishOnly": "pnpm run build",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "oxfmt": "^0.51.0",
    "oxlint": "^1.66.0",
    "oxlint-tsgolint": "^0.23.0",
    "tsdown": "catalog:"
  },
  "packageManager": "pnpm@11.2.2"
}
